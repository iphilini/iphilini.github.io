# Helix Planner Android - Детальне резюме проєкту

## 📋 Загальна інформація

**Назва проєкту:** Helix Planner  
**Платформа:** Android  
**Мова розробки:** Kotlin  
**Статус:** Планування  
**Дата створення:** 2025-01-XX

---

## 🎯 Мета та концепція

### Основна ідея
**Helix Planner** - це заміна Google Calendar зі спіральною візуалізацією часу. Замість традиційного табличного формату, події відображаються на спіралі, де один виток дорівнює одній добі. Це дозволяє бачити неперервність часу, ритмічність та події з різної перспективи.

**Ключова мета:** Повна заміна Google Calendar - користувач має працювати зі спіраллю, а не з табличним календарем. Всі події синхронізуються з Google Calendar, але основним інтерфейсом є спіраль.

**Основні принципи:**
- Один виток спіралі = одна доба
- Весь інтерфейс побудований навколо спіралі
- Управління спіраллю в просторі та її параметрами
- Ієрархічне закручування (доба → тиждень → місяць → рік)
- Створення подій "розтягуючи" їх вздовж спіралі
- Редагування подій безпосередньо на спіралі
- Повна двостороння синхронізація з Google Calendar
### Унікальні особливості
- **Спіральна метафора часу**: Один виток спіралі = одна доба, що показує неперервність та ритмічність часу
- **Ієрархічна спіралізація**: Вкладені спіралі для різних рівнів (доба → тиждень → місяць → рік)
- **Інтерактивне створення подій**: "Розтягування" подій вздовж спіралі для визначення тривалості
- **Пряма заміна Google Calendar**: Повна синхронізація, але інтерфейс - спіраль
- **Редагування на спіралі**: Всі операції (створення, редагування, видалення) безпосередньо на спіралі
- **Інтуїтивні жести**: Розтягування для створення, перетягування для зміни часу, тап для редагування
- **Сумісність з Google Calendar**: Повна інтеграція з існуючими календарями
- **Просторова навігація**: Управління спіраллю в 3D просторі з налаштуванням параметрів відображення

### Філософія дизайну
- **Візуальна перспектива**: Час як спіраль, а не лінія - дозволяє бачити паттерни та ритми
- **Мінімалізм**: Інтерфейс побудований навколо спіралі, без зайвих елементів
- **Інтуїтивність**: Створення подій через природні жести (розтягування, тап)
- **Неперервність**: Показ часу як безперервного потоку, а не дискретних блоків
- **Персоналізація**: Гнучкі налаштування відображення та параметрів спіралі

---

## 🏗️ Архітектура системи

### Загальна архітектура
```
Clean Architecture + MVVM Pattern

┌─────────────────────────────────────┐
│   Presentation Layer (UI)           │
│   - Jetpack Compose Screens         │
│   - ViewModels                       │
│   - SpiralRenderer (Canvas)          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   Domain Layer (Business Logic)      │
│   - Use Cases                        │
│   - Domain Models                    │
│   - Repository Interfaces            │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   Data Layer                         │
│   - Repository Implementations       │
│   - Room Database                    │
│   - Firebase Sync                    │
│   - Google Calendar API              │
└─────────────────────────────────────┘
```

### Шари архітектури
- **Presentation Layer:** 
  - UI компоненти (Jetpack Compose)
  - ViewModels для управління станом
  - SpiralRenderer для малювання спіралі
  - Навігація та екрани
  
- **Domain Layer:** 
  - Бізнес-логіка (Use Cases)
  - Доменні моделі (Event, ActivityLog, SpiralConfig)
  - Інтерфейси репозиторіїв
  - Правила валідації та обчислення
  
- **Data Layer:** 
  - Реалізації репозиторіїв
  - Room Database для локального сховища
  - Firebase для синхронізації
  - Google Calendar API інтеграція
  - Мережеві сервіси

### Ключові компоненти
1. **SpiralRenderer**
   - Призначення: Малювання спіральної візуалізації часу з подіями
   - Технології: Jetpack Compose Canvas API
   - Залежності: Domain models (Event, SpiralConfig)
   - Особливості: Підтримка ієрархічних рівнів, інтерактивність, анімації

2. **EventRepository**
   - Призначення: Управління подіями (CRUD операції)
   - Технології: Room Database, Firebase Realtime Database
   - Залежності: Room DAO, Firebase API
   - Особливості: Двостороння синхронізація з Google Calendar

3. **ActivityLogRepository**
   - Призначення: Зберігання та управління логами фактичної активності
   - Технології: Room Database
   - Залежності: Room DAO
   - Особливості: Трекінг поточного стану, дельта-аналіз

4. **NotificationScheduler**
   - Призначення: Планування та відправка сповіщень
   - Технології: WorkManager, NotificationManager
   - Залежності: EventRepository, ActivityLogRepository
   - Особливості: Нагадування про події, запити на логування

5. **GoogleCalendarSyncService**
   - Призначення: Повна двостороння синхронізація з Google Calendar
   - Технології: Google Calendar API, OAuth 2.0
   - Залежності: EventRepository
   - Особливості: 
     - Двостороння синхронізація (локальні зміни → Google, зміни в Google → локально)
     - Конфлікт-резолюція
     - Підтримка всіх календарів користувача
     - Синхронізація категорій, кольорів, описів
     - Real-time оновлення через webhooks

6. **SpiralConfigManager**
   - Призначення: Управління параметрами відображення спіралі
   - Технології: SharedPreferences, DataStore
   - Залежності: Немає
   - Особливості: Збереження налаштувань користувача

---

## 📊 Модель даних

### Основні сутності

#### Event (Подія - Планування)
```kotlin
@Entity(tableName = "events")
data class Event(
    @PrimaryKey val id: Long,
    val name: String,                    // Назва події
    val description: String?,            // Опис (опціонально)
    val startTime: Long,                 // Початок (timestamp)
    val endTime: Long,                   // Кінець (timestamp)
    val color: String,                   // Колір у форматі HEX (#RRGGBB)
    val repeatDays: List<Int>,          // Дні повторення [0-6] (0=Неділя)
    val repeatRule: RepeatRule?,        // Правило повторення (щодня, щотижня, тощо)
    val googleCalendarId: String?,      // ID в Google Calendar (для синхронізації)
    val location: String?,              // Місце проведення
    val createdAt: Long,                 // Дата створення
    val updatedAt: Long,                // Дата останнього оновлення
    val synced: Boolean = false,        // Чи синхронізовано з хмарою
    val isPlanned: Boolean = true       // true = планування, false = лог
)

enum class RepeatRule {
    NONE, DAILY, WEEKLY, MONTHLY, YEARLY, CUSTOM
}
```

#### SpiralConfig (Конфігурація спіралі)
```kotlin
data class SpiralConfig(
    val baseRadius: Float = 50f,        // Базовий радіус спіралі
    val stepSize: Float = 10f,          // Крок спіралі (відстань між витками)
    val heightPerDay: Float = 20f,      // Висота одного витка (доби)
    val renderRangeDays: Int = 14,      // Скільки днів відображати
    val spiralLevel: SpiralLevel,       // Рівень спіралізації
    val showPlanned: Boolean = true,    // Показувати заплановані події
    val showLogged: Boolean = true,    // Показувати фактичні логи
    val showDelta: Boolean = true       // Показувати дельту
)

enum class SpiralLevel {
    DAY,      // Один виток = доба
    WEEK,     // Один виток = тиждень
    MONTH,    // Один виток = місяць
    YEAR      // Один виток = рік
}
```

### Зв'язки між сутностями
- **Event ↔ GoogleCalendar**: Один-до-одного (синхронізація через googleCalendarId)
  - Кожна подія має унікальний ID в Google Calendar
  - Зміни в одному місці синхронізуються в інше
  - Підтримка конфліктів при одночасних змінах
  
- **SpiralConfig**: Окрема конфігурація, зберігається в SharedPreferences/DataStore
  - Налаштування відображення спіралі
  - Персональні параметри користувача

---

## 🎨 Візуалізація спіралі

### Математична модель

**Тип спіралі:** Архімедова спіраль з ієрархічним закручуванням

**Базова формула (рівень ДОБА):**
```
r = baseRadius + stepSize * θ
x = r * cos(θ)
y = r * sin(θ)
z = (час - базова_дата) * heightPerDay / (24 * 3600 * 1000)

де:
- θ = (час - базова_дата) / масштаб_часу (в радіанах)
- baseRadius = базовий радіус спіралі
- stepSize = відстань між витками
- heightPerDay = висота одного витка (доби)
```

**Ієрархічне закручування:**
- **Рівень ТИЖДЕНЬ**: Добові спіралі закручуються навколо тижневої осі
- **Рівень МІСЯЦЬ**: Тижневі спіралі закручуються навколо місячної осі
- **Рівень РІК**: Місячні спіралі закручуються навколо річної осі

**Параметри:**
- `baseRadius`: Базовий радіус (50-200 пікселів)
- `stepSize`: Крок спіралі (5-20 пікселів)
- `heightPerDay`: Висота одного витка (10-50 пікселів)
- `renderRangeDays`: Діапазон відображення (7-90 днів)
- `spiralLevel`: Рівень ієрархії (DAY/WEEK/MONTH/YEAR)

### Візуальні елементи
- **Спіраль:** 
  - Тонка сіра лінія з позначками початку днів
  - Більш яскраві позначки для початку тижнів/місяців
  - Плавна крива з можливістю збільшення/зменшення масштабу
  
- **Події (Планування):**
  - Кольорові сегменти вздовж спіралі
  - Товщина відповідає тривалості
  - Прозорість для перекриваючих подій
  - Іконки або міні-прев'ю для важливих подій
  
- **Маркер "Зараз":**
  - Яскрава точка/диск на спіралі
  - Пульсуюча анімація
  - Завжди видимий, автоматичне прокручування до нього
  
- **Інтерактивність:**
  - **Тап на подію** → відкриття деталей/редагування
  - **Довгий тап + рух** → створення нової події "розтягуванням"
  - **Перетягування події** → зміна часу початку/кінця
  - **Тап на кінці події + рух** → зміна тривалості
  - **Довгий тап на подію** → контекстне меню (видалити, дублювати, тощо)
  - **Пінч-жест** → збільшення/зменшення масштабу
  - **Свайп** → прокрутка по часу
  - **Обертання** → зміна рівня ієрархії
  - **Подвійний тап** → швидке редагування назви

### Рендеринг
**Технологія:** Jetpack Compose Canvas API

**Процес:**
1. Обчислення позицій подій на спіралі
2. Малювання базової спіралі (лінія з позначками)
3. Малювання подій (сегменти вздовж спіралі)
4. Малювання маркера "Зараз"
5. Обробка touch events для інтерактивності
6. Візуальний відгук при взаємодії (підсвічування, перетягування)

**Оптимізація:**
- Рендеринг тільки видимих подій (culling)
- Кешування обчислених позицій
- Використання Path для плавних кривих
- Анімації через Compose Animation API

---

## 🔧 Технологічний стек

### Основні технології
- **Kotlin:** 1.9+ - основна мова розробки
- **Jetpack Compose:** 1.5+ - сучасний UI фреймворк для всіх екранів
- **Room:** 2.6+ - локальна база даних для подій та логів
- **Firebase:** 
  - Realtime Database - синхронізація між пристроями
  - Authentication - аутентифікація користувачів
  - Cloud Messaging - push-сповіщення
- **Hilt:** 2.48+ - Dependency Injection
- **Coroutines + Flow:** 1.7+ - асинхронність та реактивні потоки даних
- **WorkManager:** 2.9+ - фонові задачі (сповіщення, синхронізація)
- **Google Calendar API:** - інтеграція з Google Calendar
- **Material Design 3:** - дизайн система

### Бібліотеки та залежності
```kotlin
// Core
implementation("androidx.core:core-ktx:1.12.0")
implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
implementation("androidx.activity:activity-compose:1.8.2")

// Compose
implementation("androidx.compose.ui:ui:1.5.4")
implementation("androidx.compose.material3:material3:1.1.2")
implementation("androidx.compose.ui:ui-tooling-preview:1.5.4")
implementation("androidx.navigation:navigation-compose:2.7.6")

// Room
implementation("androidx.room:room-runtime:2.6.1")
implementation("androidx.room:room-ktx:2.6.1")
kapt("androidx.room:room-compiler:2.6.1")

// Hilt
implementation("com.google.dagger:hilt-android:2.48")
kapt("com.google.dagger:hilt-compiler:2.48")
implementation("androidx.hilt:hilt-navigation-compose:1.1.0")

// Coroutines
implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")

// Firebase
implementation("com.google.firebase:firebase-database-ktx:20.3.0")
implementation("com.google.firebase:firebase-auth-ktx:22.3.0")
implementation("com.google.firebase:firebase-messaging-ktx:23.4.0")

// Google Calendar API
implementation("com.google.api-client:google-api-client-android:2.2.0")
implementation("com.google.apis:google-api-services-calendar:v3-rev20231130-2.0.0")

// WorkManager
implementation("androidx.work:work-runtime-ktx:2.9.0")

// DataStore (для налаштувань)
implementation("androidx.datastore:datastore-preferences:1.0.0")

// Testing
testImplementation("junit:junit:4.13.2")
androidTestImplementation("androidx.test.ext:junit:1.1.5")
androidTestImplementation("androidx.compose.ui:ui-test-junit4:1.5.4")
```

---

## 📱 Функціональність

### MVP (Мінімальний продукт)
- [ ] Базова спіральна візуалізація (рівень ДОБА)
- [ ] Створення подій через інтерактивне "розтягування"
- [ ] Редагування подій безпосередньо на спіралі (перетягування, зміна тривалості)
- [ ] Видалення подій
- [ ] Локальне збереження (Room Database)
- [ ] Маркер поточного часу ("Зараз")
- [ ] Базовий UI з Material Design 3
- [ ] Налаштування діапазону відображення
- [ ] Діалог редагування події (назва, час, колір, опис)

### Основні функції
- [ ] Повторювані події (щодня, щотижня, щомісяця, щороку)
- [ ] Сповіщення про заплановані події
- [ ] Налаштування параметрів спіралі (радіус, крок, висота)
- [ ] Зміна рівня ієрархії (доба → тиждень → місяць → рік)
- [ ] Пошук та фільтрація подій
- [ ] Темна тема
- [ ] Кастомізація кольорів та стилів

### Розширені функції (Google Calendar інтеграція)
- [ ] Аутентифікація через Google Account
- [ ] Двостороння синхронізація з Google Calendar
  - [ ] Завантаження існуючих подій з Google Calendar
  - [ ] Створення подій в Google Calendar при додаванні на спіралі
  - [ ] Оновлення подій в Google Calendar при редагуванні на спіралі
  - [ ] Видалення подій в Google Calendar при видаленні на спіралі
  - [ ] Відстеження змін в Google Calendar (webhooks або polling)
  - [ ] Оновлення локальних подій при змінах в Google Calendar
- [ ] Підтримка всіх календарів користувача (робочий, особистий, тощо)
- [ ] Синхронізація категорій, кольорів, описів, місць
- [ ] Конфлікт-резолюція при одночасних змінах
- [ ] Офлайн режим з синхронізацією при підключенні
- [ ] Експорт/імпорт даних (JSON, iCal)
- [ ] Віджети для домашнього екрану

---

## 🔄 Потоки даних

### Створення події (інтерактивне "розтягування")
```
1. Користувач робить довгий тап на спіралі в точці початку
2. Система показує маркер початку та дозволяє рухати палець
3. Користувач "розтягує" подію вздовж спіралі до точки кінця
4. Система обчислює час початку та кінця на основі позиції на спіралі
5. Відкривається діалог редагування (назва, колір, повторення)
6. Після збереження → збереження в Room → синхронізація з Firebase → оновлення UI
```

### Редагування події на спіралі
```
1. Користувач тапає на подію на спіралі
2. Відкривається діалог редагування з деталями події
3. Користувач змінює назву, час, колір, опис
4. Збереження → оновлення в Room → синхронізація з Google Calendar → оновлення UI

АЛЬТЕРНАТИВНО (інтерактивне редагування):
1. Користувач перетягує подію вздовж спіралі
2. Система обчислює новий час початку на основі позиції
3. Автоматичне оновлення в Room → синхронізація з Google Calendar → оновлення UI

АЛЬТЕРНАТИВНО (зміна тривалості):
1. Користувач тапає на кінець події та перетягує
2. Система обчислює нову тривалість
3. Автоматичне оновлення в Room → синхронізація з Google Calendar → оновлення UI
```

### Синхронізація з Firebase
```
1. Локальна зміна (створення/оновлення/видалення події)
2. Позначення запису як "не синхронізовано" (synced = false)
3. WorkManager запускає фонову задачу синхронізації
4. Перевірка конфліктів (якщо інший пристрій змінив ту саму подію)
5. Застосування стратегії конфлікт-резолюції (останній виграє / merge)
6. Оновлення в Firebase Realtime Database
7. Позначення як синхронізовано (synced = true)
8. Сповіщення інших пристроїв через FCM про зміни
```

### Синхронізація з Google Calendar (Двостороння)
```
ПЕРВИННА СИНХРОНІЗАЦІЯ:
1. Користувач авторизується через Google OAuth
2. Отримання токену доступу до Google Calendar API
3. Завантаження всіх календарів користувача
4. Завантаження подій з Google Calendar за вибраний період
5. Створення локальних Event з googleCalendarId
6. Відображення на спіралі

СИНХРОНІЗАЦІЯ ЛОКАЛЬНИХ ЗМІН → GOOGLE:
1. Користувач створює/редагує/видаляє подію на спіралі
2. Збереження в Room Database
3. Позначення як "не синхронізовано" (synced = false)
4. WorkManager запускає фонову задачу синхронізації
5. Якщо подія нова → створення в Google Calendar через API
6. Якщо подія існує → оновлення в Google Calendar через API
7. Якщо подія видалена → видалення в Google Calendar через API
8. Оновлення googleCalendarId в локальній базі
9. Позначення як синхронізовано (synced = true)

СИНХРОНІЗАЦІЯ ЗМІН В GOOGLE → ЛОКАЛЬНО:
1. Відстеження змін через Google Calendar API (polling кожні 5-10 хв)
   АБО через Push Notifications (webhooks) якщо доступно
2. Виявлення нових/оновлених/видалених подій
3. Перевірка конфліктів (чи не змінювалась та сама подія локально)
4. Якщо конфлікт → застосування стратегії резолюції
5. Оновлення локальних подій в Room Database
6. Оновлення візуалізації спіралі

КОНФЛІКТ-РЕЗОЛЮЦІЯ:
- Стратегія: "Останній виграє" (last-write-wins)
- Або: Показувати обидві версії та дозволити користувачу вибрати
- Або: Пріоритет Google Calendar (якщо зміна була в Google)
```

### Сповіщення
```
1. Планування сповіщень:
   - При створенні події → створення WorkRequest для нагадування
   - WorkManager планує сповіщення за X хвилин до початку події
   
2. Нагадування про подію:
   - WorkManager запускає задачу в призначений час
   - Створення Notification з деталями події
   - Дії: "Відкрити", "Відкласти", "Позначити як виконано"
   
3. Нагадування про зміни в Google Calendar:
   - "Нова подія додана в Google Calendar"
   - "Подія оновлена в Google Calendar"
```

---

## 🎯 Користувацькі сценарії

### Сценарій 1: Створення події через "розтягування"
**Користувач:** 
- Відкриває додаток, бачить спіраль з поточним часом
- Робить довгий тап на спіралі в точці "за 2 години від зараз"
- Рухає палець вздовж спіралі до точки "через 4 години"
- Бачить попередній перегляд події (кольоровий сегмент)
- Відпускає палець → відкривається діалог редагування
- Вводить назву "Зустріч з командою", вибирає колір, зберігає

**Система:** 
- Обчислює час початку та кінця на основі позицій на спіралі
- Показує інтерактивний попередній перегляд
- Відкриває діалог з заповненими полями часу
- Валідує дані (початок < кінець)
- Зберігає в Room Database
- Планує сповіщення
- Синхронізує з Firebase та Google Calendar (якщо увімкнено)
- Оновлює візуалізацію спіралі

**Результат:** 
- Подія з'являється на спіралі як кольоровий сегмент
- З'являється в списку подій
- Синхронізується з іншими пристроями та Google Calendar
- Нагадування буде надіслано за 15 хвилин до початку

### Сценарій 2: Редагування події перетягуванням
**Користувач:**
- Бачить подію "Зустріч" на спіралі
- Робить довгий тап на події та починає перетягувати
- Перетягує подію вздовж спіралі на 2 години вперед
- Відпускає палець

**Система:**
- Визначає нову позицію на спіралі
- Обчислює новий час початку та кінця (зберігаючи тривалість)
- Оновлює подію в Room Database
- Синхронізує з Google Calendar
- Оновлює візуалізацію спіралі

**Результат:**
- Подія переміщена на новий час
- Оновлена в Google Calendar
- Візуалізація оновлена миттєво

### Сценарій 3: Зміна тривалості події
**Користувач:**
- Бачить подію "Зустріч" тривалістю 1 година
- Тапає на кінець події (праву частину сегмента)
- Перетягує кінець вздовж спіралі, збільшуючи тривалість до 2 годин
- Відпускає палець

**Система:**
- Визначає нову позицію кінця на спіралі
- Обчислює нову тривалість
- Оновлює endTime в Room Database
- Синхронізує з Google Calendar
- Оновлює візуалізацію

**Результат:**
- Тривалість події збільшена
- Оновлена в Google Calendar
- Візуалізація оновлена

### Сценарій 4: Синхронізація з Google Calendar
**Користувач:**
- Відкриває додаток вперше
- Авторизується через Google Account
- Бачить повідомлення "Синхронізація з Google Calendar..."

**Система:**
- Завантажує всі календарі користувача
- Завантажує події за останні 30 днів та наступні 90 днів
- Створює локальні Event з googleCalendarId
- Відображає на спіралі
- Налаштовує періодичну синхронізацію (кожні 5-10 хв)

**Результат:**
- Всі події з Google Calendar відображаються на спіралі
- Користувач може працювати з ними як з локальними
- Зміни автоматично синхронізуються

### Сценарій 5: Зміна рівня ієрархії спіралі
**Користувач:**
- За замовчуванням бачить рівень "ДОБА" (один виток = доба)
- Обертає спіраль жестом або натискає кнопку "Рівень: ТИЖДЕНЬ"
- Спіраль трансформується: один виток = тиждень
- Бачить тижневі паттерни та ритми

**Система:**
- Змінює SpiralConfig.spiralLevel
- Перераховує позиції подій для нового рівня
- Анімує перехід між рівнями
- Агрегує події (групує по тижнях/місяцях)
- Оновлює візуалізацію

**Результат:**
- Користувач бачить час з іншої перспективи
- Може аналізувати довгострокові паттерни
- Краще розуміє ритми свого життя

---

## 🔐 Безпека та приватність

### Захист даних
- **Локальне сховище:** 
  - Room Database з шифруванням (SQLCipher опціонально)
  - SharedPreferences для налаштувань (без чутливих даних)
  
- **Мережева передача:**
  - HTTPS для всіх API запитів
  - Шифрування даних в Firebase (на рівні бази даних)
  - OAuth 2.0 для Google Calendar API
  
- **Приватність:**
  - Дані зберігаються локально, синхронізація опціональна
  - Користувач контролює, що синхронізувати
  - Можливість використання без інтернету

### Аутентифікація
- **Google Sign-In:**
  - OAuth 2.0 через Google Sign-In API
  - Токени зберігаються в EncryptedSharedPreferences
  - Автоматичне оновлення токенів
  
- **Firebase Authentication:**
  - Інтеграція з Google Account
  - Анонімна аутентифікація (опціонально)
  - Керування сесіями

- **Локальний режим:**
  - Можливість використання без аутентифікації
  - Дані тільки локально, без синхронізації

### Дозволи
- **INTERNET** - для синхронізації з Firebase та Google Calendar
- **POST_NOTIFICATIONS** - для сповіщень про події та запитів на логування
- **SCHEDULE_EXACT_ALARM** - для точних нагадувань (Android 12+)
- **READ_CALENDAR / WRITE_CALENDAR** - для синхронізації з Google Calendar (опціонально)
- **VIBRATE** - для тактильного відгуку при сповіщеннях

---

## 📈 Продуктивність

### Оптимізації
- **Рендеринг спіралі:**
  - Culling - малювання тільки видимих подій
  - Кешування обчислених позицій
  - Використання Path для плавних кривих замість окремих ліній
  - LOD (Level of Detail) - менше деталей при віддаленні
  
- **База даних:**
  - Індекси на часто використовуваних полях (startTime, endTime)
  - Пагінація при завантаженні подій
  - Фонові операції через Coroutines
  
- **Мережа:**
  - Batch синхронізація (групування змін)
  - Кешування відповідей API
  - Експоненційний backoff при помилках
  
- **Пам'ять:**
  - Використання Flow для реактивного підходу
  - Очищення кешів при нестачі пам'яті
  - Оптимізація зображень та кольорів

### Обмеження
- **Продуктивність рендерингу:**
  - Велика кількість подій (>1000) може сповільнити рендеринг
  - Рішення: агрегація та спрощення при віддаленні
  
- **Синхронізація:**
  - Конфлікти при одночасних змінах на різних пристроях
  - Рішення: стратегія конфлікт-резолюції (останній виграє / merge)
  
- **Google Calendar API:**
  - Обмеження кількості запитів (quota)
  - Рішення: кешування та batch операції
  
- **Google Calendar API:**
  - Обмеження кількості запитів (quota)
  - Рішення: кешування, batch операції, оптимізація частоти синхронізації

---

## 🧪 Тестування

### Стратегія тестування
- **TDD підхід:** Написання тестів перед реалізацією для критичної логіки
- **Покриття коду:** Мінімум 70% для бізнес-логіки (Domain layer)
- **Автоматизація:** CI/CD pipeline з автоматичним запуском тестів
- **Ручне тестування:** UI/UX тестування на реальних пристроях

### Типи тестів
- **Unit тести:**
  - Математика спіралі (обчислення позицій)
  - Use Cases (бізнес-логіка)
  - Repository логіка (моки бази даних)
  - Google Calendar синхронізація (моки API)
  - Конфлікт-резолюція
  
- **Integration тести:**
  - Room Database операції
  - Firebase синхронізація (тестовий проєкт)
  - Google Calendar API (mock або тестовий аккаунт)
  - WorkManager задачі
  
- **UI тести:**
  - Compose UI компоненти (ComposeTestRule)
  - Навігація між екранами
  - Інтерактивність спіралі (тап, жести)
  - Діалоги та форми
  
- **E2E тести:**
  - Повний цикл створення події
  - Синхронізація з Google Calendar
  - Редагування подій на спіралі

---

## 📦 Розгортання

### Збірка
- **Debug збірка:** Для розробки та тестування
- **Release збірка:** 
  - ProGuard/R8 для мінімізації та обфускації
  - Підписання APK/AAB
  - Оптимізація ресурсів
  
- **CI/CD:**
  - GitHub Actions / GitLab CI
  - Автоматична збірка при push
  - Автоматичне тестування
  - Автоматичне розгортання в Google Play (Internal Testing)

### Публікація
- **Google Play Store:**
  - Основна платформа публікації
  - Стадії: Internal Testing → Closed Testing → Open Testing → Production
  - Material Design 3 для кращого інтегрування
  
- **F-Droid (опціонально):**
  - Для користувачів, які віддають перевагу open-source
  - Потребує видалення залежностей від Google Play Services (якщо можливо)
  
- **Пряме розповсюдження:**
  - APK через веб-сайт (для бета-тестерів)

---

## 🗺️ Дорожня карта

### Phase 1: MVP (Мінімальний продукт)
**Термін:** 6-8 тижнів  
**Цілі:**
- Базова спіральна візуалізація (рівень ДОБА)
- Створення/редагування/видалення подій
- Інтерактивне "розтягування" для створення подій
- Локальне збереження (Room Database)
- Список подій (альтернативний вигляд)
- Маркер "Зараз"
- Базовий UI (Material Design 3)
- Налаштування діапазону відображення

### Phase 2: Google Calendar інтеграція
**Термін:** 4-6 тижнів  
**Цілі:**
- Аутентифікація через Google Account
- Первинна синхронізація (завантаження подій з Google Calendar)
- Створення подій в Google Calendar при додаванні на спіралі
- Оновлення подій в Google Calendar при редагуванні
- Видалення подій в Google Calendar при видаленні
- Відстеження змін в Google Calendar
- Конфлікт-резолюція
- Підтримка всіх календарів користувача

### Phase 3: Розширені функції
**Термін:** 4-6 тижнів  
**Цілі:**
- Повторювані події (з синхронізацією в Google Calendar)
- Сповіщення про події
- Налаштування параметрів спіралі
- Ієрархічні рівні спіралі (тиждень, місяць, рік)
- Темна тема
- Кастомізація кольорів та стилів
- Пошук та фільтрація подій

### Phase 4: Поліпшення та оптимізація
**Термін:** 4-6 тижнів  
**Цілі:**
- Оптимізація продуктивності рендерингу
- Покращення анімацій та переходів
- Розширена статистика
- Віджети
- Додаткові експорти (iCal, CSV)
- Публікація в Google Play

---

## 📝 Додаткові нотатки

### Альтернативна реалізація ієрархічної спіралі (Frenet Frames)

**Джерело:** React/Three.js реалізація з використанням Frenet Frames для багаторівневої спіралі

**Ключові концепції, які можуть бути корисними:**

1. **Frenet Frames для ієрархічного закручування:**
   - Кожен рівень (S0/S1/S2/S3) обертається навколо попереднього
   - Використання локальної системи координат (tangent, normal, binormal)
   - Математично коректне обчислення позицій
   - **Застосування:** Може бути використано для Phase 3 (ієрархічні рівні) замість простішого підходу

2. **Архітектура обчислень:**
   ```kotlin
   // Приклад структури для Android
   class SpiralCalculator {
       fun getS3Position(t: Float): Vector3  // Річна спіраль
       fun getS3Frame(t: Float): FrenetFrame
       fun getS2Position(t: Float): Vector3  // Місячна (відносно S3)
       fun getS2Frame(t: Float): FrenetFrame
       fun getS1Position(t: Float): Vector3  // Денна (відносно S2)
       fun getS1Frame(t: Float): FrenetFrame
       fun getS0Position(t: Float): Vector3  // Годинна (відносно S1)
   }
   ```
   - **Перевага:** Чітке розділення відповідальності, легко тестувати
   - **Застосування:** Можна використати цю структуру для організації коду

3. **Оптимізація рендерингу:**
   - Різна кількість сегментів для різних рівнів (500 для S3, 1000 для S2, 800 для S1)
   - Адаптивна деталізація залежно від рівня
   - **Застосування:** LOD (Level of Detail) для продуктивності на мобільних

4. **Система контролів:**
   - Динамічна зміна параметрів (hoursPerDay, daysPerMonth, monthsPerYear, numberOfYears)
   - Увімкнення/вимкнення рівнів (showS0, showS1, showS2, showS3)
   - **Застосування:** Налаштування спіралі в Android додатку

5. **Інтерактивність:**
   - Velocity-based обертання (плавне продовження руху після відпускання)
   - Zoom з обмеженнями (50-300)
   - Touch events з обробкою одночасних жестів
   - **Застосування:** Покращення UX в Android версії

6. **Візуальні елементи:**
   - Маркери з glow ефектами
   - Sprites для текстових міток
   - Різні кольори для рівнів (S0=жовтий, S1=червоний, S2=синій, S3=зелений)
   - **Застосування:** Візуальне покращення спіралі

**Рішення для MVP:**
- Для MVP використовуємо простішу Архімедову спіраль (рівень ДОБА)
- Frenet Frames підхід залишаємо для Phase 3 (ієрархічні рівні)
- Архітектуру обчислень (розділення на функції) використовуємо з самого початку

**Майбутнє покращення:**
- Коли додаватимуться ієрархічні рівні, можна мігрувати на Frenet Frames підхід
- Це дасть більш математично коректну та візуально привабливу спіраль

### Ключові відмінності від веб-версій
- **Нативна реалізація:** Замість WebView використовується Jetpack Compose для кращої продуктивності
- **Повна заміна Google Calendar:** Додаток не доповнення, а повноцінна заміна зі спіральним інтерфейсом
- **Інтерактивне редагування:** Редагування подій безпосередньо на спіралі через жести
- **Ієрархічність:** Розширена підтримка різних рівнів спіралізації (доба → тиждень → місяць → рік)
- **Двостороння синхронізація:** Повна інтеграція з Google Calendar - зміни в обох напрямках

### Майбутні можливості (не в MVP)
- AI-аналіз паттернів та рекомендації
- Інтеграція з іншими календарями (Outlook, Apple Calendar)
- Спільні календарі (сімейні, командні)
- Експорт у різні формати для аналізу
- Кастомні теми та стилі спіралі
- 3D візуалізація (як в веб-версіях)
- Голосовий трекінг (якщо буде потрібно в майбутньому)

### Технічні виклики
- **Продуктивність рендерингу:** Велика кількість подій (>1000) може сповільнити малювання
  - Рішення: оптимізація через culling та LOD
  
- **Конфлікти синхронізації:** Одночасні зміни в додатку та Google Calendar
  - Рішення: стратегія конфлікт-резолюції (last-write-wins або користувацький вибір)
  
- **Google Calendar API обмеження:** Quota на кількість запитів
  - Рішення: кешування, batch операції, оптимізація частоти синхронізації
  
- **Офлайн режим:** Робота без інтернету з подальшою синхронізацією
  - Рішення: локальне сховище + черга синхронізації

### Дизайн-система
- **Кольори:** Material Design 3 Color System з підтримкою темної теми
- **Типографіка:** Material Design 3 Typography Scale
- **Компоненти:** Material Design 3 компоненти (Buttons, Cards, Dialogs)
- **Анімації:** Material Motion для плавних переходів

---

## 🔗 Посилання та ресурси

- [Посилання на дизайн]
- [Посилання на документацію]
- [Інші корисні ресурси]

---

**Останнє оновлення:** 2025-01-XX  
**Автор:** iphilini
